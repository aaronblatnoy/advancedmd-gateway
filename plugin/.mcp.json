{
  "mcpServers": {
    "amd-patients": {
      "command": "uvx",
      "args": [
        "advancedmd-mcp",
        "--domain",
        "patients"
      ],
      "env": {
        "ADVANCEDMD_GATEWAY_URL": "${ADVANCEDMD_GATEWAY_URL}",
        "ADVANCEDMD_GATEWAY_TOKEN": "${ADVANCEDMD_GATEWAY_TOKEN}"
      }
    },
    "amd-visits": {
      "command": "uvx",
      "args": [
        "advancedmd-mcp",
        "--domain",
        "visits"
      ],
      "env": {
        "ADVANCEDMD_GATEWAY_URL": "${ADVANCEDMD_GATEWAY_URL}",
        "ADVANCEDMD_GATEWAY_TOKEN": "${ADVANCEDMD_GATEWAY_TOKEN}"
      }
    },
    "amd-providers": {
      "command": "uvx",
      "args": [
        "advancedmd-mcp",
        "--domain",
        "providers"
      ],
      "env": {
        "ADVANCEDMD_GATEWAY_URL": "${ADVANCEDMD_GATEWAY_URL}",
        "ADVANCEDMD_GATEWAY_TOKEN": "${ADVANCEDMD_GATEWAY_TOKEN}"
      }
    },
    "amd-codes": {
      "command": "uvx",
      "args": [
        "advancedmd-mcp",
        "--domain",
        "codes"
      ],
      "env": {
        "ADVANCEDMD_GATEWAY_URL": "${ADVANCEDMD_GATEWAY_URL}",
        "ADVANCEDMD_GATEWAY_TOKEN": "${ADVANCEDMD_GATEWAY_TOKEN}"
      }
    },
    "amd-billing": {
      "command": "uvx",
      "args": [
        "advancedmd-mcp",
        "--domain",
        "billing"
      ],
      "env": {
        "ADVANCEDMD_GATEWAY_URL": "${ADVANCEDMD_GATEWAY_URL}",
        "ADVANCEDMD_GATEWAY_TOKEN": "${ADVANCEDMD_GATEWAY_TOKEN}"
      }
    },
    "amd-payments": {
      "command": "uvx",
      "args": [
        "advancedmd-mcp",
        "--domain",
        "payments"
      ],
      "env": {
        "ADVANCEDMD_GATEWAY_URL": "${ADVANCEDMD_GATEWAY_URL}",
        "ADVANCEDMD_GATEWAY_TOKEN": "${ADVANCEDMD_GATEWAY_TOKEN}"
      }
    },
    "amd-masterfiles": {
      "command": "uvx",
      "args": [
        "advancedmd-mcp",
        "--domain",
        "masterfiles"
      ],
      "env": {
        "ADVANCEDMD_GATEWAY_URL": "${ADVANCEDMD_GATEWAY_URL}",
        "ADVANCEDMD_GATEWAY_TOKEN": "${ADVANCEDMD_GATEWAY_TOKEN}"
      }
    },
    "amd-system": {
      "command": "uvx",
      "args": [
        "advancedmd-mcp",
        "--domain",
        "system"
      ],
      "env": {
        "ADVANCEDMD_GATEWAY_URL": "${ADVANCEDMD_GATEWAY_URL}",
        "ADVANCEDMD_GATEWAY_TOKEN": "${ADVANCEDMD_GATEWAY_TOKEN}"
      }
    },
    "amd-ehr": {
      "command": "uvx",
      "args": [
        "advancedmd-mcp",
        "--domain",
        "ehr"
      ],
      "env": {
        "ADVANCEDMD_GATEWAY_URL": "${ADVANCEDMD_GATEWAY_URL}",
        "ADVANCEDMD_GATEWAY_TOKEN": "${ADVANCEDMD_GATEWAY_TOKEN}"
      }
    }
  }
}
