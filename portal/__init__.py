"""AdvancedMD portal computer-use sidecar (Playwright + optional LLM recovery).

UI-only flows that the XML API does not cover. Runs outside the gateway
entry/request queues on a separate port (default 8821).
"""

__all__ = ["__version__"]

__version__ = "1.0.0"
