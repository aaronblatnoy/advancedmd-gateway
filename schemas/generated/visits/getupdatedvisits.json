{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "amd-mcp/schemas/visits/getupdatedvisits",
  "title": "AMD action: getupdatedvisits",
  "x-amd-action": {
    "domain": "visits",
    "key": "getupdatedvisits",
    "name": "getupdatedvisits",
    "phi_touching": true,
    "tier": 2,
    "write": false
  },
  "description": "List visits whose record changed at or after a checkpoint (datechanged = prior response servertime; alias since). Returns datechanged, servertime, count, group-bys, and a visits list sorted newest-updated first.",
  "type": "object",
  "anyOf": [
    {"required": ["datechanged"]},
    {"required": ["since"]}
  ],
  "properties": {
    "datechanged": {
      "type": "string",
      "minLength": 1,
      "description": "Prior response servertime checkpoint (ISO 8601). Echo verbatim."
    },
    "since": {
      "type": "string",
      "minLength": 1,
      "description": "Alias for datechanged (deprecated Adam-facing name)."
    }
  }
}
